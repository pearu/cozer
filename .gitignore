# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# report preview scratch
review/

# cozer runtime artifacts
log/
cozer_tex_*/
Untitled.coz
*.aux
*.dvi

# editors / OS
.DS_Store
*.swp

# cozer event working files (auto-created next to opened .coz; never commit)
*.cozj
*.cozj.journal
*.cozj.bak*
# ...except the bundled seed rulesets shipped with the package
!cozer/rulesets/*.cozj
cozer-crashes/

# generated report PDFs (auto-created in <event>.reports/; never commit)
*.reports/
