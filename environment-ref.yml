name: cozer-ref
# Throwaway Python 2.7 environment used ONLY to run the legacy core headless
# (via tools/wx_shim.py) to generate golden reference fixtures. Not for the GUI.
# See MAINTENANCE_PLAN.md §6.
channels:
  - conda-forge
dependencies:
  - python=2.7
