"""PySide6 desktop GUI for cozer (Phase 5).

The GUI is a thin layer over the equivalence-proven core (analyzer/racepattern),
the crash-safe persistence layer (cozer.store), and the report pipeline
(cozer.reports). Recording durability lives in the store, not here.
"""
